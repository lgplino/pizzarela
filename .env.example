# Local / Vercel — use Postgres (Neon, Supabase, Vercel Postgres…)
# On Neon, prefer the *pooled* connection string for serverless.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# Optional: secret for POST /api/seed (default: pizzarela-seed)
SEED_SECRET="pizzarela-seed"
